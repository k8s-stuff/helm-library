apiVersion: v2
name: example-app
description: Example application chart that uses the helm-library library chart
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: helm-library
    version: ">=1.0.0"
    repository: "file://../../charts/helm-library"
